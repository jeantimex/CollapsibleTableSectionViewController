language: objective-c
osx_image: xcode12
env:
  global:
  - LC_CTYPE=en_US.UTF-8
  - LANG=en_US.UTF-8
  - IOS_SDK=iphonesimulator14.0
script:
  - xcodebuild -showsdks
  - xcodebuild test -project CollapsibleTableSectionViewController.xcodeproj -scheme 'CollapsibleTableSectionViewController' -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 11, OS=14' build test
after_success:
  - bash <(curl -s https://codecov.io/bash)
